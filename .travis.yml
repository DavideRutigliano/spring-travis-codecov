language: java

jdk:
 - oraclejdk8

cache:
  directories:
    - $HOME/.m2

before_install:
 - chmod +x gradlew

install: true
script:
  - ./gradlew clean install
  - bash <(curl -s https://codecov.io/bash)
